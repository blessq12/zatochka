# 🏗️ Подробное пояснение архитектуры проекта

## 📋 Содержание

1. [Общая архитектура](#общая-архитектура)
2. [Доменный слой](#доменный-слой)
3. [Инфраструктурный слой](#инфраструктурный-слой)
4. [Слой приложения](#слой-приложения)
5. [Паттерны проектирования](#паттерны-проектирования)
6. [Поток данных](#поток-данных)
7. [Примеры использования](#примеры-использования)

---

## 🎯 Общая архитектура

### Что это такое?

Ваш проект построен по принципу **Domain-Driven Design (DDD)** с использованием **Clean Architecture**. Это означает, что бизнес-логика (домен) находится в центре и не зависит от внешних технологий.

### Зачем это нужно?

-   **Разделение ответственности** - каждый слой отвечает за свою часть
-   **Независимость от фреймворков** - можно легко заменить Laravel на другой
-   **Тестируемость** - бизнес-логику можно тестировать отдельно
-   **Масштабируемость** - легко добавлять новые функции

### Структура слоёв:

```
┌─────────────────────────────────────┐
│           Presentation              │ ← Веб-интерфейс, API
├─────────────────────────────────────┤
│           Application               │ ← Сценарии использования
├─────────────────────────────────────┤
│             Domain                  │ ← Бизнес-логика (САМОЕ ВАЖНОЕ!)
├─────────────────────────────────────┤
│         Infrastructure             │ ← База данных, внешние сервисы
└─────────────────────────────────────┘
```

---

## 🧠 Доменный слой

### Что это?

Доменный слой содержит **бизнес-логику** - правила, которые определяют, как работает ваше приложение. Это "мозг" системы.

### Компоненты домена:

#### 1. **Entity (Сущность) - User.php**

```php
class User implements AggregateRoot
```

**Что делает:**

-   Представляет пользователя в системе
-   Содержит бизнес-правила (например, нельзя изменить удалённого пользователя)
-   Управляет своим состоянием

**Как работает:**

-   При создании проверяет валидность данных
-   Методы изменения состояния (`activate()`, `deactivate()`) содержат бизнес-логику
-   Нельзя создать невалидного пользователя

**Пример использования:**

```php
$user = User::register($userId, "Иван", $email, $passwordHash);
$user->activate(); // Активирует пользователя
$user->assignRoles(['admin']); // Назначает роли
```

#### 2. **Value Objects (Объекты-значения)**

##### **UserId.php**

```php
class UserId
```

**Что делает:**

-   Представляет уникальный идентификатор пользователя
-   Гарантирует, что ID всегда валидный UUID
-   Нельзя изменить после создания

**Как работает:**

-   `new()` - создаёт новый UUID
-   `fromString()` - создаёт из строки с валидацией
-   `equals()` - сравнивает два ID

**Пример:**

```php
$userId = UserId::new(); // Создаёт новый UUID
$userId2 = UserId::fromString("123e4567-e89b-12d3-a456-426614174000");
```

##### **Email.php**

```php
class Email
```

**Что делает:**

-   Представляет email адрес
-   Автоматически нормализует (приводит к нижнему регистру)
-   Проверяет формат email

**Как работает:**

-   `fromString()` - создаёт из строки с валидацией
-   Всегда хранит нормализованную версию
-   Нельзя создать невалидный email

**Пример:**

```php
$email = Email::fromString("User@EXAMPLE.com"); // Сохранится как "user@example.com"
```

##### **PasswordHash.php**

```php
class PasswordHash
```

**Что делает:**

-   Представляет хеш пароля
-   Обеспечивает безопасное хранение паролей
-   Нельзя получить исходный пароль

**Как работает:**

-   `fromHash()` - создаёт из готового хеша
-   Хранит только хешированную версию
-   Используется для сравнения паролей

#### 3. **Domain Service (Доменный сервис) - UserDomainService.php**

```php
class UserDomainService
```

**Что делает:**

-   Координирует сложные бизнес-операции
-   Работает с несколькими агрегатами
-   Публикует доменные события

**Как работает:**

-   Принимает зависимости через конструктор (Repository, Hasher, Roles, Events)
-   Метод `register()` создаёт пользователя, назначает роли и публикует событие
-   Обрабатывает бизнес-правила (например, проверка уникальности email)

**Пример:**

```php
$user = $userService->register("Иван", "ivan@example.com", "password123", ['user']);
```

#### 4. **Domain Events (Доменные события)**

##### **UserRegistered.php**

```php
class UserRegistered
```

**Что делает:**

-   Уведомляет систему о том, что пользователь зарегистрирован
-   Содержит данные о событии (ID, имя, email)
-   Иммутабельный (нельзя изменить после создания)

**Как работает:**

-   Создаётся в `UserDomainService` после успешной регистрации
-   Публикуется через `EventBus`
-   Другие части системы могут подписаться на это событие

**Пример использования:**

```php
// В другом сервисе можно подписаться на событие
$this->events->subscribe(UserRegistered::class, function($event) {
    // Отправить приветственное письмо
    $this->emailService->sendWelcome($event->email);
});
```

#### 5. **Policies (Политики) - UserPolicy.php**

```php
class UserPolicy
```

**Что делает:**

-   Определяет, кто может выполнять какие действия
-   Содержит бизнес-правила доступа
-   Работает с Laravel Spatie Permission

**Как работает:**

-   `viewAny()` - кто может просматривать список пользователей
-   `update()` - кто может изменять пользователя
-   `assignRoles()` - кто может назначать роли
-   `delete()` - кто может удалять пользователей

**Пример:**

```php
if ($policy->update($currentUser, $targetUser)) {
    // Пользователь может изменять
} else {
    // Доступ запрещён
}
```

---

## 🔧 Инфраструктурный слой

### Что это?

Инфраструктурный слой отвечает за **технические детали** - как сохранять данные, как работать с внешними сервисами.

### Компоненты инфраструктуры:

#### 1. **Repository (Репозиторий) - EloquentUserRepository.php**

```php
class EloquentUserRepository implements UserRepositoryInterface
```

**Что делает:**

-   Реализует интерфейс `UserRepositoryInterface`
-   Работает с базой данных через Eloquent
-   Преобразует доменные объекты в модели базы данных

**Как работает:**

-   `nextId()` - генерирует новый UUID
-   `save()` - сохраняет пользователя в базу
-   `getById()` - получает пользователя по ID
-   `getByEmail()` - получает пользователя по email
-   `existsByEmail()` - проверяет существование email

**Пример:**

```php
$user = $repository->getById($userId);
$repository->save($user);
```

#### 2. **Mapper (Маппер) - UserMapper.php**

```php
class UserMapper
```

**Что делает:**

-   Преобразует доменные объекты в модели Eloquent и обратно
-   Скрывает различия между доменной моделью и моделью базы данных

**Как работает:**

-   `toDomain()` - преобразует Eloquent модель в доменный объект
-   `toModel()` - преобразует доменный объект в Eloquent модель
-   Обрабатывает различия в именовании полей

**Пример:**

```php
$domainUser = UserMapper::toDomain($eloquentModel);
$eloquentModel = UserMapper::toModel($domainUser);
```

#### 3. **Eloquent Model (Модель) - UserModel.php**

```php
class UserModel extends Authenticatable
```

**Что делает:**

-   Представляет таблицу `users` в базе данных
-   Наследует от Laravel Authenticatable для аутентификации
-   Использует Spatie Permission для управления ролями

**Как работает:**

-   `HasRoles` trait для работы с ролями
-   `HasApiTokens` для API аутентификации
-   `HasFactory` для создания тестовых данных
-   Кастомный guard `manager` для аутентификации

#### 4. **Infrastructure Services (Инфраструктурные сервисы)**

##### **LaravelPasswordHasher.php**

```php
class LaravelPasswordHasher implements PasswordHasherInterface
```

**Что делает:**

-   Адаптирует Laravel Hash для доменного слоя
-   Реализует интерфейс `PasswordHasherInterface`
-   Обеспечивает безопасное хеширование паролей

**Как работает:**

-   `hash()` - хеширует пароль через `Hash::make()`
-   `verify()` - проверяет пароль через `Hash::check()`
-   Скрывает детали реализации Laravel от домена

##### **RoleServiceSpatie.php**

```php
class RoleServiceSpatie implements RoleServiceInterface
```

**Что делает:**

-   Адаптирует Spatie Permission для доменного слоя
-   Реализует интерфейс `RoleServiceInterface`
-   Управляет ролями пользователей

**Как работает:**

-   `assignRoles()` - назначает роли через `syncRoles()`
-   `getRoles()` - получает роли пользователя
-   Работает с Eloquent моделью, но скрывает это от домена

---

## 🚀 Слой приложения

### Что это?

Слой приложения содержит **сценарии использования** - как пользователи взаимодействуют с системой.

### Компоненты приложения:

#### 1. **Application Services (Сервисы приложения)**

Пока пустые, но предназначены для:

-   Координации между доменными сервисами
-   Управления транзакциями
-   Обработки пользовательских запросов

#### 2. **Unit of Work (Единица работы)**

Пока не реализован, но предназначен для:

-   Управления транзакциями базы данных
-   Обеспечения согласованности данных
-   Отката изменений при ошибках

---

## 🎨 Паттерны проектирования

### 1. **Repository Pattern (Репозиторий)**

**Что делает:**

-   Скрывает детали работы с базой данных
-   Предоставляет единый интерфейс для доступа к данным
-   Позволяет легко заменить способ хранения данных

**Как работает в вашем проекте:**

```php
// Интерфейс определяет контракт
interface UserRepositoryInterface {
    public function save(User $user): void;
    public function getById(UserId $id): ?User;
}

// Реализация скрывает детали Eloquent
class EloquentUserRepository implements UserRepositoryInterface {
    // Работает с базой данных
}
```

### 2. **Factory Method (Фабричный метод)**

**Что делает:**

-   Создаёт объекты с определёнными параметрами
-   Проверяет валидность при создании
-   Предоставляет разные способы создания

**Как работает в вашем проекте:**

```php
// Создание нового пользователя
$user = User::register($userId, $name, $email, $passwordHash);

// Восстановление из базы данных
$user = User::reconstitute($userId, $name, $email, $passwordHash, $isActive, $isDeleted, $roles);
```

### 3. **Value Object Pattern (Объект-значение)**

**Что делает:**

-   Представляет простые значения с валидацией
-   Иммутабельный (нельзя изменить после создания)
-   Гарантирует корректность данных

**Как работает в вашем проекте:**

```php
// Email всегда валидный и нормализованный
$email = Email::fromString("User@EXAMPLE.com");
echo $email; // Выведет: user@example.com

// UserId всегда валидный UUID
$userId = UserId::fromString("invalid"); // Выбросит исключение
```

### 4. **Domain Event Pattern (Доменное событие)**

**Что делает:**

-   Уведомляет систему об изменениях в домене
-   Позволяет слабо связанным компонентам взаимодействовать
-   Обеспечивает расширяемость системы

**Как работает в вашем проекте:**

```php
// После регистрации пользователя
$this->events->publish(new UserRegistered($userId, $name, $email));

// Другие сервисы могут подписаться на это событие
// Например, отправить приветственное письмо
```

### 5. **Dependency Injection (Внедрение зависимостей)**

**Что делает:**

-   Передаёт зависимости через конструктор
-   Упрощает тестирование
-   Обеспечивает слабую связанность

**Как работает в вашем проекте:**

```php
class UserDomainService {
    public function __construct(
        private readonly UserRepositoryInterface $users,
        private readonly PasswordHasherInterface $hasher,
        private readonly RoleServiceInterface $roles,
        private readonly EventBusInterface $events
    ) {}
}
```

---

## 🔄 Поток данных

### Как работает регистрация пользователя:

```
1. HTTP Request → Controller
    ↓
2. Controller → UserDomainService::register()
    ↓
3. UserDomainService создаёт Email, UserId, PasswordHash
    ↓
4. Проверяет уникальность email через Repository
    ↓
5. Создаёт доменный объект User::register()
    ↓
6. Сохраняет пользователя через Repository
    ↓
7. Назначает роли через RoleService
    ↓
8. Публикует событие UserRegistered
    ↓
9. Возвращает созданного пользователя
```

### Как работает получение пользователя:

```
1. HTTP Request → Controller
    ↓
2. Controller → Repository::getById()
    ↓
3. Repository получает данные из базы через Eloquent
    ↓
4. Mapper преобразует Eloquent модель в доменный объект
    ↓
5. Возвращается доменный объект User
```

---

## 💡 Примеры использования

### Создание пользователя:

```php
// В контроллере или другом сервисе
$user = $userDomainService->register(
    "Иван Иванов",
    "ivan@example.com",
    "password123",
    ['user']
);

// Пользователь автоматически:
// - Получает уникальный UUID
// - Проверяется уникальность email
// - Пароль хешируется
// - Назначаются роли
// - Публикуется событие регистрации
```

### Изменение пользователя:

```php
// Получаем пользователя
$user = $repository->getById($userId);

// Изменяем данные
$user->rename("Пётр Петров");
$user->changeEmail(Email::fromString("petr@example.com"));

// Сохраняем изменения
$repository->save($user);
```

### Проверка прав доступа:

```php
// В контроллере
if ($userPolicy->update($currentUser, $targetUser)) {
    // Пользователь может изменять
    $user->rename($newName);
    $repository->save($user);
} else {
    // Доступ запрещён
    throw new AccessDeniedException();
}
```

---

## 🎯 Ключевые принципы

### 1. **Инверсия зависимостей**

-   Доменный слой не зависит от внешних слоёв
-   Внешние слои зависят от домена через интерфейсы

### 2. **Единственная ответственность**

-   Каждый класс отвечает за одну вещь
-   User отвечает за пользователя, Email за email

### 3. **Инкапсуляция**

-   Внутреннее состояние защищено
-   Изменения происходят через публичные методы

### 4. **Иммутабельность**

-   Value Objects нельзя изменить после создания
-   Гарантирует корректность данных

### 5. **Расширяемость**

-   Легко добавлять новые функции
-   Можно заменить реализации через интерфейсы

---

## 🚀 Следующие шаги

### Что можно улучшить:

1. **Добавить Unit of Work** для управления транзакциями
2. **Реализовать Event Sourcing** для аудита изменений
3. **Добавить CQRS** для разделения операций чтения и записи
4. **Реализовать Saga Pattern** для сложных бизнес-процессов

### Что уже хорошо:

1. ✅ Чёткое разделение слоёв
2. ✅ Использование паттернов DDD
3. ✅ Слабая связанность компонентов
4. ✅ Хорошая тестируемость
5. ✅ Соответствие принципам SOLID

---

## 📚 Полезные ссылки

-   [Domain-Driven Design](https://martinfowler.com/bliki/DomainDrivenDesign.html)
-   [Clean Architecture](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
-   [Repository Pattern](https://martinfowler.com/eaaCatalog/repository.html)
-   [Value Objects](https://martinfowler.com/bliki/ValueObject.html)

---

_Этот документ поможет вам лучше понять архитектуру проекта и принципы, на которых он построен._ 🎉
