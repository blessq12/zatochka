---
description: Архитектурные правила и подходы разработки для проекта Zatochka
globs:
alwaysApply: true
---

# 🏗️ Архитектурные правила проекта Zatochka

## 🎯 Основная архитектура

Проект построен на **Domain-Driven Design (DDD)** с использованием **Clean Architecture** и **Event-Driven Architecture**. Следуй принципам "Банды четырёх" и современным паттернам проектирования.

### Структура слоёв:

```
┌─────────────────────────────────────┐
│           Presentation              │ ← Filament, API, Controllers
├─────────────────────────────────────┤
│           Application               │ ← Use Cases, Application Services
├─────────────────────────────────────┤
│             Domain                  │ ← Entities, Value Objects, Services
├─────────────────────────────────────┤
│         Infrastructure              │ ← Repositories, Mappers, Events
└─────────────────────────────────────┘
```

## 🧠 Доменный слой (Domain)

### Entities (Сущности)

-   **Реализуй AggregateRoot** для основных сущностей
-   **Используй фабричные методы**: `register()`, `reconstitute()`
-   **Инкапсулируй бизнес-логику** в методах сущности
-   **Публикуй доменные события** при изменении состояния

```php
class User implements AggregateRoot
{
    public static function register(UserId $id, string $name, Email $email, PasswordHash $hash): self
    public function activate(): void
    public function assignRoles(array $roles): void
}
```

### Value Objects (Объекты-значения)

-   **Иммутабельные** - нельзя изменить после создания
-   **Валидация в конструкторе** - выбрасывай исключения при невалидных данных
-   **Статические фабричные методы**: `fromString()`, `fromInteger()`, `zero()`
-   **Методы сравнения**: `equals()`, `isGreaterThan()`

```php
class Email
{
    private function __construct(private string $value) {}
    public static function fromString(string $email): self
    public function equals(Email $other): bool
}
```

### Domain Services

-   **Координируют сложные операции** между агрегатами
-   **Принимают зависимости через конструктор**
-   **Публикуют доменные события** через EventBus

## 🚀 Слой приложения (Application)

### Use Cases / Application Services

-   **Один класс = один сценарий использования**
-   **Используй `handle()` метод** для основной логики
-   **Принимай зависимости через конструктор**
-   **Работай с доменными объектами**, не с Eloquent моделями

```php
final class CreateOrder
{
    public function __construct(
        private readonly OrderRepositoryInterface $orders,
        private readonly EventBusInterface $events
    ) {}

    public function handle(CreateOrderRequest $request): Order
    {
        // Логика создания заказа
    }
}
```

## 🔧 Инфраструктурный слой (Infrastructure)

### Repository Pattern

-   **Реализуй интерфейсы** из доменного слоя
-   **Используй Mapper** для преобразования между доменом и Eloquent
-   **Скрывай детали работы с БД** от домена

```php
class EloquentUserRepository implements UserRepositoryInterface
{
    public function __construct(private readonly UserMapper $mapper) {}

    public function save(User $user): void
    {
        $model = $this->mapper->toEloquent($user);
        $model->save();
    }
}
```

### Mapper Pattern

-   **Статические методы**: `toDomain()`, `toEloquent()`
-   **Преобразуй Value Objects** в примитивы и обратно
-   **Обрабатывай различия** в именовании полей

### Event-Driven Architecture

-   **Публикуй события** в доменных сервисах
-   **Подписывайся на события** в Infrastructure
-   **Используй EventBus** для слабой связанности

```php
// В доменном сервисе
$this->events->publish(new UserRegistered($userId, $name, $email));

// В подписчике
class UserRegisteredSubscriber
{
    public function handle(UserRegistered $event): void
    {
        // Обработка события
    }
}
```

## 🎨 Паттерны проектирования

### Обязательные паттерны:

1. **Repository** - для доступа к данным
2. **Factory Method** - для создания объектов
3. **Value Object** - для простых значений
4. **Domain Event** - для уведомлений
5. **Dependency Injection** - для зависимостей
6. **Observer** - для событийной архитектуры
7. **Strategy** - для алгоритмов
8. **Command** - для Use Cases

### Принципы SOLID:

-   **S** - Single Responsibility (одна ответственность)
-   **O** - Open/Closed (открыт для расширения, закрыт для изменения)
-   **L** - Liskov Substitution (заменяемость)
-   **I** - Interface Segregation (разделение интерфейсов)
-   **D** - Dependency Inversion (инверсия зависимостей)

## 📁 Структура файлов

```
app/
├── Domain/                    # Доменная логика
│   ├── Users/                # Бизнес-домен
│   │   ├── Entities/         # Сущности
│   │   ├── ValueObjects/     # Объекты-значения
│   │   ├── Services/         # Доменные сервисы
│   │   ├── Events/           # Доменные события
│   │   └── Policies/         # Политики доступа
│   └── Shared/               # Общие компоненты
├── Application/              # Сценарии использования
│   ├── Users/               # Use Cases
│   └── Orders/
├── Infrastructure/           # Техническая реализация
│   ├── Persistence/         # Работа с данными
│   │   ├── Eloquent/        # Eloquent репозитории
│   │   └── Mappers/         # Мапперы
│   ├── Events/              # Обработка событий
│   └── Services/            # Инфраструктурные сервисы
└── Filament/                # UI слой
```

## 🚫 Что НЕ делать

-   ❌ **Не смешивай слои** - домен не должен знать об Infrastructure
-   ❌ **Не используй Eloquent модели** в доменном слое
-   ❌ **Не делай сущности изменяемыми** без бизнес-логики
-   ❌ **Не создавай God Objects** - следуй принципу единственной ответственности
-   ❌ **Не игнорируй события** - используй их для слабой связанности

## ✅ Что делать

-   ✅ **Используй Value Objects** для всех примитивных типов
-   ✅ **Публикуй доменные события** при изменении состояния
-   ✅ **Создавай интерфейсы** для всех внешних зависимостей
-   ✅ **Тестируй доменную логику** отдельно от Infrastructure
-   ✅ **Следуй принципам DDD** и Clean Architecture
-   ✅ **Используй юмор и маты** в комментариях (по желанию 😄)

## 🔥 Примеры из проекта

### Создание пользователя:

```php
$user = $userDomainService->register(
    "Иван Иванов",
    "ivan@example.com",
    "password123",
    ['user']
);
```

### Value Object с валидацией:

```php
$email = Email::fromString("User@EXAMPLE.com"); // → "user@example.com"
$quantity = Quantity::fromInteger(5);
$money = Money::fromFloat(100.50, 'RUB');
```

### Событийная архитектура:

```php
// Публикация события
$this->events->publish(new UserRegistered($userId, $name, $email));

// Подписка на событие
$eventBus->subscribe(UserRegistered::class, [$subscriber, 'handle']);
```

---

**Помни**: Архитектура должна быть понятной, тестируемой и расширяемой. Домен - это сердце системы, всё остальное - детали реализации! 🎯
