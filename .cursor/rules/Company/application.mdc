---
description: Company — application layer (текущий код)
globs: app/Application/Company/**
alwaysApply: false
---

# Company / Application

## Commands

`SaveBranchCommand` + `SaveBranchHandler`

## Queries

`GetPublicSiteContentQuery` + `GetPublicSiteContentQueryHandler`

Возвращает: contacts, schedule, company, delivery_info, faq — из `SiteContentRepositoryInterface::getValuesByKeys`.
