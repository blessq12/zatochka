---
description: Identity — domain layer (текущий код)
globs: app/Domain/Identity/**,app/Infrastructure/Identity/**,app/Models/User.php
alwaysApply: false
---

# Identity / Domain

- **Entity:** `Master` (доменное имя мастера)
- **Repository:** `MasterRepositoryInterface`

Persistence/auth: `UserModel` + `App\Models\User` — не domain entity, infrastructure adapter.

`Order.masterId` в OrderFulfillment ссылается на `users.id`.

## Нет в коде

Policies, POS token issuance handlers
