---
description: Identity — application layer (текущий код)
globs: app/Application/Identity/**
alwaysApply: false
---

# Identity / Application

## Commands

| Command | Handler |
|---------|---------|
| `RegisterMasterCommand` | `RegisterMasterHandler` |
| `UpdateMasterCommand` | `UpdateMasterHandler` |

Filament `CreateMaster` / `EditMaster` / `CreateManager` вызывают handlers.

## Не в Application

POS login — `PosController::login` (Presentation), напрямую `UserModel` + Sanctum.

## Cross-BC read

- `PosOrderReadModelBuilder` → `MasterRepositoryInterface`
- `OrderDocumentReadModelBuilder` → `MasterRepositoryInterface`
