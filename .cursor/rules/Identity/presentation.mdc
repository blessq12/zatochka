---
description: Identity — presentation (текущий код)
globs: app/Http/**,routes/**,config/auth.php
alwaysApply: false
---

# Identity / Presentation

## Сейчас

- `config/auth.php` — guards `web`, `client`
- Нет login endpoints для POS / Filament

## Правило

Не использовать `User` для публичного клиентского API — только `ClientAuthModel`.
