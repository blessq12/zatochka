---
description: Warehouse — domain layer (текущий код)
globs: app/Domain/Warehouse/**
alwaysApply: false
---

# Warehouse / Domain

- **Entity:** `WarehouseItem`, `StockMovement`
- **Enum:** `StockMovementType`
- **Repository:** `WarehouseItemRepositoryInterface` (find/save), `StockMovementRepositoryInterface` (save only)

Автосписание при `AddMaterialToOrder` — **не** в коде (ES: MVP вручную).
