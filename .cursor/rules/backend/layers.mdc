---
description: Слоистая архитектура (BC-first DDD + Hexagonal)
alwaysApply: true
---

# Слои

Presentation → Application → Domain ← Infrastructure

BC-first: `app/Domain/{BC}/`, `Application/{BC}/`, `Infrastructure/{BC}/`.

Cross-BC: только `*_id` в Domain; orchestration — [flows.mdc](../flows.mdc).

Контексты: [contexts.mdc](../contexts.mdc). Тесты: `php artisan test` (~44).
