---
description: Catalog — domain layer (текущий код)
globs: app/Domain/Catalog/**
alwaysApply: false
---

# Catalog / Domain

## Классы

- **Entity:** `Branch`, `PriceBlock`, `PriceItem`, `SiteSetting`
- **Enum:** `PriceType`
- **Repository:** 4 interface — find/save; `SiteSettingRepositoryInterface::findByKey`

## Нет в коде

Статический `getValue()` (был на старой Eloquent-модели) — только через repository
