---
description: Catalog — application layer (текущий код)
globs: app/Application/Catalog/**
alwaysApply: false
---

# Catalog / Application

## Сейчас

Пустые папки Command/Query/…

## Целевое (ES)

Query `GetPublicBootstrap` — агрегация прайсов + `SiteSetting` для публичного SPA.

Filament CRUD — через handlers или напрямую через repos (решить при реализации).
