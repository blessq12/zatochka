---
description: ClientPortal — domain layer (текущий код)
globs: app/Domain/ClientPortal/**
alwaysApply: false
---

# ClientPortal / Domain

## Классы

- **Entity:** `Client`, `SiteLead`, `Review`
- **Enum:** `ReviewStatus` — pending, approved, rejected
- **Repository:** `ClientRepositoryInterface`, `SiteLeadRepositoryInterface`, `ReviewRepositoryInterface`
- **Event:** `SiteLeadReceived`, `ClientRegistered`, `GuestOrdersLinkedToClient`, `ReviewSubmitted`, `ReviewApproved`, `ReviewRejected`
- **Exception:** `SiteLeadPolicyViolation`, `ClientAlreadyRegisteredException`, `ClientNotFoundException`, `ReviewPolicyViolation`

## Поведение

- `Client::register()`, `updateProfile()`, `markPasswordSet()`, `assignId()`
- `SiteLead::create()`, `markConverted(int $orderId)` — POL-09, один раз; `assignId()`
- `Review::submit()`, `approve()`, `reject()`, `assignId()` — POL: отзыв только на `issued` заказ

## Семантика ES

| ES | Код |
|----|-----|
| Lead (заявка) | `SiteLead`, table `site_leads` |
| `lead_id` на Order | `SiteLead.id` после конвертации |

## Cross-BC

Проверка заказа при отзыве — в Application (`SubmitReviewHandler` → `OrderRepositoryInterface`), не в entity Review напрямую.
