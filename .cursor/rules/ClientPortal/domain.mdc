---
description: ClientPortal — domain layer (текущий код)
globs: app/Domain/ClientPortal/**
alwaysApply: false
---

# ClientPortal / Domain

## Классы

- **Entity:** `Client`, `SiteLead`, `Review`
- **Enum:** `ReviewStatus` — pending, approved, rejected
- **Repository:** `ClientRepositoryInterface`, `SiteLeadRepositoryInterface`, `ReviewRepositoryInterface`

## Поля SiteLead (entity)

`converted`, `orderId` — есть в entity/migration; логика конвертации **не** в Application.

## Нет в коде

Events, валидация SubmitReview (issued), auth use cases
