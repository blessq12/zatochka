---
description: ClientPortal — application layer (текущий код)
globs: app/Application/ClientPortal/**
alwaysApply: false
---

# ClientPortal / Application

## Сейчас

Пустые папки Command/CommandHandler/Query/QueryHandler/Presenter.

## Целевое (ES)

`SubmitSiteLead`, `RegisterClient`, `UpdateClientProfile`, `SubmitReview`

Cross-BC: `SubmitReview` проверяет `order_id` + статус issued через OrderFulfillment port.
