---
description: OrderFulfillment — presentation (текущий код)
globs: app/Http/**,routes/**
alwaysApply: false
---

# OrderFulfillment / Presentation

## Сейчас

- `routes/web.php` — SPA catch-all (`MainController`)
- `routes/api.php` — заглушка `GET /api/user`
- **Нет** контроллеров заказов, Filament, POS

## Целевые каналы (ES)

- Filament `/cp` — менеджер
- POS `/api/pos/*` — мастер (фильтр по `master_id`)
