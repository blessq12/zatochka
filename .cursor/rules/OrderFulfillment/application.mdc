---
description: OrderFulfillment — application layer (текущий код)
globs: app/Application/OrderFulfillment/**
alwaysApply: false
---

# OrderFulfillment / Application

## Сейчас

Папки `Command/`, `CommandHandler/`, `Query/`, `QueryHandler/`, `Presenter/` — **пустые** (`.gitkeep`).

## Целевые use cases (ES, не в коде)

`CreateOrder`, `AssignMasterToOrder`, `TakeOrderToWork`, `IssueOrder`, `RecalculateOrderPrice`, …

## Правило

Handler инжектит `OrderRepositoryInterface` и порты других BC, не Eloquent.
