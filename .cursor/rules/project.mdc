---
description: Запуск проекта
alwaysApply: false
---

# Проект

Laravel-монолит, DDD + Hexagonal.

```bash
composer install && cp .env.example .env && php artisan key:generate && php artisan migrate --seed
```

Демо: `root@root.com` / `master@master.com` — password.
