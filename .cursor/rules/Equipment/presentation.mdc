---
description: Equipment — presentation (текущий код)
globs: app/Http/**,app/Filament/**,routes/**
alwaysApply: false
---

# Equipment / Presentation

## Filament `/cp`

- `Equipment/EquipmentResource` — список + `CreateEquipment` (через `RegisterEquipmentHandler`)
- `Orders/OrderManageActions::linkEquipment()` — Select + `LinkEquipmentToOrder`

## POS `/api/pos/*`

| Метод | Путь |
|-------|------|
| GET | `equipment?query=&page=` |
| GET | `equipment/{equipmentId}/orders` |

Read-only для мастера (история ремонтов).
