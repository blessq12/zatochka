---
description: Equipment — application layer (текущий код)
globs: app/Application/Equipment/**
alwaysApply: false
---

# Equipment / Application

## Command

`RegisterEquipmentCommand` + `RegisterEquipmentHandler` → `EquipmentRegistered`

## Queries

| Query | Назначение |
|-------|------------|
| `SearchEquipment` | поиск по name/brand/model/SN, пагинация |
| `GetEquipmentOrderHistory` | заказы по `equipment_id` (cross-BC → `OrderRepositoryInterface`) |

## Presenter

`EquipmentPresenter` — POS API

## Cross-BC

`LinkEquipmentToOrderHandler` — в `Application/OrderFulfillment/`
