# Команды для работы с демо-данными

Этот документ описывает команды для управления демо-данными в приложении "Заточка Про".

## 🚀 Создание демо-данных

### Основная команда

```bash
php artisan app:fill-demo-data
```

### Создание с перезаписью

```bash
php artisan app:fill-demo-data --force
```

**Что создается:**

-   2 менеджера (Анна Менеджерова, Сергей Управляющий)
-   2 мастера (Михаил Мастеров, Алексей Ремонтников)
-   2 филиала (Центральный офис, Филиал на Ленина)
-   10 клиентов с Telegram-аккаунтами
-   7 заказов (4 заточки + 3 ремонта)
-   3 ремонта для заказов типа repair
-   10 отзывов (7 на заказы + 3 общих о сервисе)
-   10 бонусных карт для клиентов

**Данные для входа:**

-   **Менеджеры:** `anna.manager@zatochka-pro.ru` / `password`
-   **Мастера:** `mikhail.master@zatochka-pro.ru` / `password`
-   **Клиенты:** любой номер телефона / `password`

## 🧹 Очистка демо-данных

### Очистка только демо-данных (рекомендуется)

```bash
php artisan app:clear-demo-data
```

### Очистка без подтверждения

```bash
php artisan app:clear-demo-data --force
```

**Что удаляется:**

-   Все клиенты
-   Все заказы
-   Все ремонты
-   Все отзывы
-   Все бонусы клиентов
-   Все бонусные транзакции
-   Все уведомления
-   Все Telegram сообщения и чаты
-   Демо-пользователи (менеджеры и мастера)
-   Демо-филиалы
-   Демо-компании

**Что остается:**

-   Системные данные (типы заказов, статусы, настройки бонусов)
-   Сидерные данные (FAQ, типы доставки, оплаты и т.д.)
-   Системный пользователь (root@root.com)

## 🔄 Полная очистка базы данных

### Очистка только демо-данных

```bash
php artisan app:reset-database --demo-only
```

### Полная очистка БД

```bash
php artisan app:reset-database
```

### Полная очистка + запуск сидеров

```bash
php artisan app:reset-database --seed
```

### Полная очистка без подтверждения

```bash
php artisan app:reset-database --force
```

## 📋 Типичные сценарии использования

### Для разработки

```bash
# Создать демо-данные для тестирования
php artisan app:fill-demo-data

# Очистить после тестирования
php artisan app:clear-demo-data --force
```

### Для демонстрации

```bash
# Подготовить чистую БД с системными данными
php artisan app:reset-database --seed

# Добавить демо-данные
php artisan app:fill-demo-data
```

### Для сдачи проекта

```bash
# Полная очистка до дефолтного состояния
php artisan migrate:fresh --seed

# Или пошагово:
php artisan app:reset-database --force
php artisan db:seed
```

## ⚠️ Важные замечания

1. **Команды необратимы** - всегда делайте бэкап перед использованием
2. **Используйте `--force`** только когда уверены в своих действиях
3. **Для продакшена** используйте только `--demo-only` флаги
4. **Сидеры** создают базовые системные данные (типы, настройки)
5. **Демо-данные** создают тестовых пользователей и заказы

## 🛠️ Архитектурные особенности

Команды используют следующие паттерны:

-   **Command** - сами команды
-   **Builder** - для создания связанных данных
-   **Factory Method** - для создания разных типов объектов
-   **Strategy** - для разных типов заказов
-   **Transaction** - для атомарности операций

## 📊 Структура создаваемых данных

```
Компания "Заточка Про"
├── Филиалы (2 шт.)
│   ├── Центральный офис
│   └── Филиал на Ленина
├── Пользователи (4 шт.)
│   ├── Менеджеры (2 шт.)
│   └── Мастера (2 шт.)
├── Клиенты (10 шт.)
│   ├── С заказами (7 шт.)
│   └── Без заказов (3 шт.)
├── Заказы (7 шт.)
│   ├── Заточка (4 шт.)
│   └── Ремонт (3 шт.)
├── Ремонты (3 шт.)
│   └── Привязаны к заказам типа repair
└── Отзывы (10 шт.)
    ├── На заказы (7 шт.)
    └── Общие о сервисе (3 шт.)
```

## 🔧 Техническая информация

-   **Файлы команд:** `app/Console/Commands/`
-   **Фабрики:** `database/factories/`
-   **Миграции:** `database/migrations/`
-   **Сидеры:** `database/seeders/`

Все команды безопасны для использования и не затрагивают системные данные при правильном использовании флагов.
