# 🗄️ Полный флоу работы с базой данных

## 📋 Доступные команды

### 1. **Очистка и миграция БД**

```bash
php artisan db:flow fresh
```

-   Удаляет все таблицы и создает заново
-   Запускает все миграции
-   **ВНИМАНИЕ**: Удаляет ВСЕ данные!

### 2. **Запуск сидеров**

```bash
php artisan db:flow seed
```

-   Запускает все сидеры для создания базовых данных
-   Создает пользователей, настройки, справочники

### 3. **Добавление демо данных**

```bash
php artisan db:flow demo
```

-   Добавляет демо клиентов, заказы, отзывы
-   Создает тестовые данные для разработки

### 4. **Удаление демо данных**

```bash
php artisan db:flow clean-demo
```

-   Удаляет только демо данные
-   Сохраняет базовые настройки и пользователей

### 5. **Полный сброс (все операции)**

```bash
php artisan db:flow full-reset
```

-   Очистка БД + сидеры + демо данные
-   Полная переинициализация системы

## 🚀 Быстрый старт

### Для разработки (с демо данными):

```bash
php artisan db:flow full-reset
```

### Для продакшена (только базовые данные):

```bash
php artisan db:flow fresh
php artisan db:flow seed
```

## 🔑 Данные для входа

### Администратор:

-   **Email**: `admin@zatochka.org`
-   **Пароль**: `password`
-   **Роль**: `admin`

### Демо пользователь:

-   **Email**: `demo@zatochka.org`
-   **Пароль**: `demo123`
-   **Роль**: `user`

## 📊 Что создается сидерами

### UserSeeder

-   Администратор системы
-   Демо пользователь

### CompanySeeder

-   Компания "Заточка Томск"
-   Главный филиал с контактами

### BonusSettingSeeder

-   Настройки бонусной системы
-   Проценты, курсы обмена, сроки

### Справочники (PaymentType, ServiceType, etc.)

-   Типы оплаты (4 типа)
-   Типы услуг (5 типов)
-   Статусы заказов (12 статусов)
-   Типы доставки (4 типа)
-   Типы оборудования (5 типов)

### FaqSeeder

-   7 часто задаваемых вопросов
-   Ответы по услугам и гарантиям

## 🎭 Демо данные

### FillDemoDataCommand создает:

-   **Пользователи**: менеджеры и мастера
-   **Клиенты**: 20+ тестовых клиентов
-   **Заказы**: различные статусы и типы
-   **Отзывы**: положительные и отрицательные
-   **Бонусы**: история начислений и списаний

## ⚠️ Важные моменты

1. **Всегда делайте бэкап** перед `fresh` или `full-reset`
2. **Используйте `--force`** для автоматического выполнения без подтверждений
3. **Демо данные** можно удалить командой `clean-demo`
4. **Базовые настройки** сохраняются при удалении демо

## 🔧 Дополнительные команды

### Стандартные Laravel команды:

```bash
php artisan migrate:fresh --seed  # Очистка + сидеры
php artisan db:seed              # Только сидеры
php artisan migrate:reset        # Откат миграций
```

### Наша команда:

```bash
php artisan db:flow full-reset --force  # Без подтверждений
```
