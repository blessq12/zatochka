# Проблемы для исправления - Заточка

## 🚨 **КРИТИЧНЫЕ ПРОБЛЕМЫ**

### 1. **Unit тесты не проходят (17 failed, 12 passed)**

**Проблемы:**

-   `Client::factory()` не существует - нужны фабрики для тестов
-   Таблица `order_statuses` не существует в тестовой БД
-   `CalculationService` возвращает неправильные множители (1.0 вместо 1.5 и 1.3)
-   Много предупреждений о deprecated doc-comments в PHPUnit

**Файлы для исправления:**

-   `tests/Unit/Services/CalculationServiceTest.php`
-   `tests/Unit/Services/ClientServiceTest.php`
-   `tests/Unit/Services/OrderServiceTest.php`
-   `database/factories/ClientFactory.php` (создать)
-   `database/factories/OrderFactory.php` (создать)

### 2. **Feature тесты не проходят (19 failed, 4 passed)**

**Проблемы:**

-   `Client::factory()` не существует во всех тестах
-   Неправильная структура JSON ответа (ожидается 'client', но возвращается другая структура)
-   Много предупреждений о deprecated doc-comments

**Файлы для исправления:**

-   `tests/Feature/Api/ClientAuthApiTest.php`
-   `tests/Feature/Api/OrderApiTest.php`
-   `app/Http/Controllers/Api/ClientController.php` (исправить структуру ответа)

### 3. **CollectStatsCommand падает**

**Проблема:**

-   Column not found 'is_approved' in reviews table

**Файлы для исправления:**

-   `app/Console/Commands/CollectStatsCommand.php`
-   `database/migrations/` (добавить колонку is_approved в reviews)

### 4. **Events/Listeners не работают**

**Проблемы:**

-   `OrderCreated` событие не логируется в логах
-   Уведомления не создаются при создании заказа

**Файлы для исправления:**

-   `app/Events/OrderCreated.php`
-   `app/Listeners/SendOrderCreatedNotification.php`
-   `app/Models/Order.php` (добавить dispatch события)
-   `app/Providers/EventServiceProvider.php` (проверить регистрацию)

### 5. ✅ **DTO классы отсутствуют** - **ВЫПОЛНЕНО**

**Проблема:**

-   `BaseDTO`, `CreateOrderDTO`, `ClientDTO` не существуют

**Файлы для создания:**

-   `app/DTO/BaseDTO.php`
-   `app/DTO/CreateOrderDTO.php`
-   `app/DTO/ClientDTO.php`

### 6. **SendReviewCreatedNotification Listener отсутствует**

**Проблема:**

-   Event `ReviewCreated` есть, но `Listener` не создан

**Файлы для создания:**

-   `app/Listeners/SendReviewCreatedNotification.php`

### 7. **Отношения в моделях**

**Проблема:**

-   `OrderStatus` отношение не существует в `Order` модели

**Файлы для исправления:**

-   `app/Models/Order.php` (добавить отношение orderStatus)

### 8. **Telegram Bot offline**

**Проблема:**

-   `HealthCheckCommand` показывает что бот недоступен

**Файлы для исправления:**

-   `config/services.php` (проверить настройки Telegram)
-   `app/Services/TelegramService.php` (проверить конфигурацию)

## ⚠️ **СРЕДНИЕ ПРОБЛЕМЫ**

### 1. **Планировщик не настроен полностью**

**Проблемы:**

-   Команда бэкапа не добавлена в планировщик
-   Команда очистки логов не добавлена в планировщик

**Файлы для исправления:**

-   `app/Console/Kernel.php` (добавить команды в schedule)

### 2. **Непротестированные команды**

**Проблемы:**

-   `DiagnoseTelegramVerificationCommand` не протестирована
-   `CleanLogsCommand` не протестирована

**Действия:**

-   Протестировать команды вручную
-   Добавить в планировщик если нужно

## 📋 **ПЛАН ИСПРАВЛЕНИЯ**

### **Приоритет 1 (Критично):**

1. ✅ Создать фабрики для тестов - **ВЫПОЛНЕНО**
2. ✅ Исправить CalculationService множители - **ВЫПОЛНЕНО**
3. ✅ Добавить колонку is_approved в reviews - **ВЫПОЛНЕНО**
4. ✅ Исправить структуру JSON ответов в API - **ВЫПОЛНЕНО**
5. ✅ Настроить Events/Listeners - **ВЫПОЛНЕНО**

### **Приоритет 2 (Важно):**

1. Создать DTO классы
2. Добавить недостающие отношения в моделях
3. Создать SendReviewCreatedNotification Listener
4. Настроить Telegram Bot

### **Приоритет 3 (Средний):**

1. Настроить планировщик для бэкапов и логов
2. Протестировать непротестированные команды

## 🎯 **ЦЕЛЬ**

Исправить все критические проблемы, чтобы:

-   Все тесты проходили успешно
-   Events/Listeners работали корректно
-   API возвращал правильные структуры данных
-   Все команды функционировали
-   Проект был готов к продакшену

---

**Готов к исправлению проблем! 🚀**
